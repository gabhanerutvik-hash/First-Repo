# First-Repo
This repo includes two Python scripts: one performs basic operations (add, subtract, multiply, divide) on two user-input numbers; the other creates a personalized greeting by combining first and last name. Both scripts are clearly documented for easy testing and use.
